export interface CoinComparison{
    "logoid":string;
    "price":string;
    "highestPrice":string;
    "highFactor":string;
    "lowestPrice":string;
    "lowFactor":string;
}
export interface CoinComparisonName{
    "logoid":string;
    "name":string;
    "symbol":string;
}